% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/airspaces.R
\name{fir_sf}
\alias{fir_sf}
\title{Retrieve FIR (Flight Information Region) airspace geometries for a given AIRAC cycle}
\usage{
fir_sf(conn = NULL, cfmu_airac)
}
\arguments{
\item{conn}{Database connection or instantiate the default one.}

\item{cfmu_airac}{the AIRAC cycle number (CFMU format), e.g. \code{"517"} or
\code{517}. Both character and numeric inputs are accepted.
See \code{\link[airac:cfmu_airac]{airac::cfmu_airac()}} to look up the cycle number for a given date.}
}
\value{
An \code{sf} object with the following columns:
\itemize{
\item \code{airac_cfmu}: the AIRAC cycle id
\item \code{icao}: the 2-letter ICAO prefix (derived from \code{code})
\item \code{id}: the airspace volume id
\item \code{code}: the FIR code
\item \code{name}: the FIR name
\item \code{min_fl}: the minimum flight level
\item \code{max_fl}: the maximum flight level
\item \code{airspace_type}: the airspace type (\code{FIR})
\item \code{geometry}: the airspace polygon geometry
}
}
\description{
Returns FIR (Flight Information Region) airspace geometries as an \code{sf} object
for the specified CFMU AIRAC cycle.

An \code{icao} column is derived from the first two characters of the airspace
\code{code}.
}
\section{Note}{
You need to either provide a connection \code{conn} that has access to
\code{ENV_SP.AIRSPACE_VOLUME}, \code{PRU_CFMU_FIR} and \code{SWH_MAP} or go with the
default which uses PRU_DEV to establish a \code{\link[=db_connection]{db_connection()}}.

The \code{sf} package is required but only suggested; install it if not available.
}

\examples{
\dontrun{
firs <- fir_sf(cfmu_airac = "517")

# write to GeoJSON
sf::st_write(firs, "ir-517.geojson", driver = "GeoJSON")

# if you re-use DB connections
conn <- eurocontrol::db_connection("PRU_DEV")
firs <- fir_sf(conn = conn, cfmu_airac = "517")

# ... do something else with conn
# ...
# then manually close the connection to the DB
DBI::dbDisconnect(conn)
}
}
\seealso{
\code{\link[airac:cfmu_airac]{airac::cfmu_airac()}} from the
\href{https://github.com/eurocontrol/airac}{airac} package.
}
