% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/airspaces.R
\name{ansp_sf}
\alias{ansp_sf}
\title{Retrieve ACE ANSP airspace geometries for a given AIRAC cycle}
\usage{
ansp_sf(conn = NULL, cfmu_airac)
}
\arguments{
\item{conn}{Database connection or instantiate the default one.}

\item{cfmu_airac}{the AIRAC cycle number (CFMU format), e.g. \code{"517"}}
}
\value{
An \code{sf} object with the following columns:
\itemize{
\item \code{airac_cfmu}: the AIRAC cycle id
\item \code{id}: the airspace identifier
\item \code{code}: the ANSP code
\item \code{name}: the ANSP name
\item \code{ace_code}: the ACE benchmarking code
\item \code{min_fl}: the minimum flight level
\item \code{max_fl}: the maximum flight level
\item \code{airspace_type}: the airspace type
\item \code{geometry}: the airspace polygon geometry
}
}
\description{
Returns ANSP (Air Navigation Service Provider) airspace geometries from the
ACE (ATM Cost-Effectiveness) benchmarking dataset as an \code{sf} object for the
specified CFMU AIRAC cycle.

Airspaces with code \code{AIRPORT}, \code{UNKNOWN} or \code{MILITARY} are excluded.
}
\section{Note}{
You need to either provide a connection \code{conn} that has access to
\code{ENV_SP.AIRSPACE}, \code{PRU_CFMU_ANSP} and \code{SWH_MAP} or go with the default
which uses PRU_READ to establish a \code{\link[=db_connection]{db_connection()}}.

The \code{sf} package is required but only suggested; install it if not available.
}

\examples{
\dontrun{
ansps <- ansp_sf(cfmu_airac = "481")

# write to GeoJSON
sf::st_write(ansps, "ansp_ace_481.geojson", driver = "GeoJSON")

# if you re-use DB connections
conn <- eurocontrol::db_connection("PRU_READ")
ansps <- ansp_sf(conn = conn, cfmu_airac = "481")

# ... do something else with conn
# ...
# then manually close the connection to the DB
DBI::dbDisconnect(conn)
}
}
